#include "console.h"
#include "videoDriver.h"
#include ""

extern int line, linePosition;

void printf(char* format, ...){
	int i = 0, parameterIndex;
	while(format != 0){
		if(format++ == '%'){
			if(format == 'd')

		}
	}
}

void putchar(char c){
	
}