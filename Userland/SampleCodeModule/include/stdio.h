#define _stdin 0
#define _stdout 1
#define _stderr 2

void printf(char* format, ...);

int scanf(char* format, ...);

void putchar(char c);

char getchar();

void write(char c, int dest);

char read(int dest);
